<UserControl x:Class="InsuranceBrokerSystem.UI.Views.Dashboard.OperationalDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary Source="/Style.xaml"/>
    </UserControl.Resources>
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Stats Cards -->
        <UniformGrid Columns="4" Margin="0,0,0,20">
            <!-- Card 1 -->
            <Border Style="{StaticResource StatCardStyle}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Total Policies" FontSize="14" Foreground="#7F8C8D" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalPolicies}" FontSize="28" Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="+15% from last month" FontSize="12" Foreground="#2ECC71" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card 2 -->
            <Border Style="{StaticResource StatCardStyle}" Margin="10,0,10,0">
                <StackPanel>
                    <TextBlock Text="Total Premium" FontSize="14" Foreground="#7F8C8D" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalPremiumStr}" FontSize="28" Foreground="{StaticResource SecondaryBrush}" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="+8% from last month" FontSize="12" Foreground="#2ECC71" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card 3 -->
            <Border Style="{StaticResource StatCardStyle}" Margin="10,0,10,0">
                <StackPanel>
                    <TextBlock Text="Active Claims" FontSize="14" Foreground="#7F8C8D" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ActiveClaims}" FontSize="28" Foreground="#E67E22" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="-2% from last month" FontSize="12" Foreground="#E74C3C" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card 4 -->
            <Border Style="{StaticResource StatCardStyle}" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="New Clients" FontSize="14" Foreground="#7F8C8D" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding NewClients}" FontSize="28" Foreground="#9B59B6" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="+24% from last month" FontSize="12" Foreground="#2ECC71" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Charts Space -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource StatCardStyle}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Premium Growth" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,15"/>
                    <Border Background="#F9FBFF" Height="250" CornerRadius="8">
                        <lvc:CartesianChart Series="{Binding TrendSeries}" 
                                            XAxes="{Binding XAxes}" 
                                            YAxes="{Binding YAxes}" 
                                            Margin="10"/>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource StatCardStyle}" Grid.Column="1" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Policies by Type" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,15"/>
                    <Border Background="#F9FBFF" Height="250" CornerRadius="8">
                        <lvc:PieChart Series="{Binding PieSeries}" 
                                      LegendPosition="Right"
                                      Margin="10"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Recent Activity -->
        <Border Grid.Row="2" Style="{StaticResource StatCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Recent Activities" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,15"/>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding RecentActivities}" HeadersVisibility="None" Background="Transparent" BorderThickness="0" RowHeight="40" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Action}" Width="2*"/>
                        <DataGridTextColumn Binding="{Binding User}" Width="*"/>
                        <DataGridTextColumn Binding="{Binding Time}" Width="*"/>
                        <DataGridTextColumn Binding="{Binding Status}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
