<Window x:Class="InsuranceBrokerSystem.UI.Views.MasterData.BankManagementWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             WindowStartupLocation="CenterOwner"
             ResizeMode="NoResize"
             Title="Bank Management">

    <Window.Resources>
        <ResourceDictionary Source="/Style.xaml"/>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Border Background="#0D3559" Padding="15,12">
            <TextBlock Text="Bank Management" 
                       Foreground="White" 
                       FontSize="18" 
                       FontWeight="SemiBold"/>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Existing Data List -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Existing Banks" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListBox x:Name="lstBanks" 
                         Height="250"
                         SelectionChanged="lstBanks_SelectionChanged"
                         DisplayMemberPath="Name"/>
            </StackPanel>

            <!-- Add/Edit Form -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Add/Edit Bank" FontWeight="Bold" Margin="0,0,0,10"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="Name:" Grid.Row="0"/>
                    <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Height="28"/>
                    
                    <Label Content="Bank Code:" Grid.Row="2"/>
                    <TextBox x:Name="txtCode" Grid.Row="2" Grid.Column="1" Height="28" MaxLength="10"/>
                    
                    <Label Content="Description:" Grid.Row="4"/>
                    <TextBox x:Name="txtDescription" Grid.Row="4" Grid.Column="1" Height="60" TextWrapping="Wrap"/>
                    
                    <Label Content="SWIFT Code:" Grid.Row="6"/>
                    <TextBox x:Name="txtSwiftCode" Grid.Row="6" Grid.Column="1" Height="28" MaxLength="11"/>
                    
                    <Label Content="Is Active:" Grid.Row="8"/>
                    <CheckBox x:Name="chkIsActive" Grid.Row="8" Grid.Column="1" VerticalAlignment="Center" IsChecked="True"/>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnAdd" Content="Add" Width="80" Height="35" Margin="5,0"
                            Style="{StaticResource SecondaryButtonStyle}" Background="#27AE60"
                            Click="btnAdd_Click"/>
                    <Button x:Name="btnUpdate" Content="Update" Width="80" Height="35" Margin="5,0"
                            Style="{StaticResource SecondaryButtonStyle}" Background="#3498DB"
                            Click="btnUpdate_Click" IsEnabled="False"/>
                    <Button x:Name="btnDelete" Content="Delete" Width="80" Height="35" Margin="5,0"
                            Style="{StaticResource SecondaryButtonStyle}" Background="#E74C3C"
                            Click="btnDelete_Click" IsEnabled="False"/>
                    <Button x:Name="btnClear" Content="Clear" Width="80" Height="35" Margin="5,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="btnClear_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15">
            <Button Content="Refresh" Width="100" Height="38" Margin="8,0"
                    Style="{StaticResource SecondaryButtonStyle}" 
                    Click="btnRefresh_Click"/>
            <Button Content="Close" Width="100" Height="38" Margin="8,0"
                    Style="{StaticResource SecondaryButtonStyle}" Background="#EF4444"
                    Click="btnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
